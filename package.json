{
  "name": "movie-picker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && node server/scripts/buildDb.js",
    "start": "node server/index.js"
  }
}
